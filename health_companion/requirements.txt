# Health Companion — runtime dependencies
# Python 3.10+ recommended

python-telegram-bot==21.6   # async Telegram bot framework (long-polling, no open ports needed)
httpx==0.27.2               # async HTTP client used for OpenRouter, Firecrawl, Nutritionix, OpenFoodFacts, Sparky
python-dotenv==1.0.1        # load configuration from a .env file
